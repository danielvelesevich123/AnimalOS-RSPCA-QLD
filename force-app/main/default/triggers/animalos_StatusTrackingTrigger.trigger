trigger animalos_StatusTrackingTrigger on animalos__Status_Tracking__c (before insert, before update, before delete, after insert, after update, after delete, after undelete) {
//    fflib_SObjectDomain.triggerHandler(animalos_StatusTrackingDomain.class);
}