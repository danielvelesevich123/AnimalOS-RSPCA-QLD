<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>aos_Job_Outcome_Rescue_Cancelled</fullName>
    <active>false</active>
    <description>When Job Outcome = Rescue Cancelled then a cancelled reason must go in the Outcome Notes.</description>
    <errorConditionFormula>AND(
    ISPICKVAL(animalos__Job_Outcome__c, &quot;Rescue Cancelled&quot;),
    ISBLANK(animalos__Outcome_Notes__c)
)</errorConditionFormula>
    <errorMessage>When Job Outcome = Rescue Cancelled then a cancelled reason must go in the Outcome Notes.</errorMessage>
</ValidationRule>
